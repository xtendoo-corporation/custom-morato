<odoo>
    <data>
        <template id="report_invoice_document_inherit"
                  inherit_id="account.report_invoice_document"
                  name="Unit Price sin IVA y 4 decimales">
            <xpath expr="//td[@name='td_price_unit']" position="replace">
                <td name="td_price_unit" t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                    <span class="text-nowrap">
                        <t t-if="line.quantity">
                            <t t-out="('{:.4f}'.format(line.price_subtotal / line.quantity))"/>
                        </t>
                        <t t-else="">0.0000</t>
                    </span>
                </td>
            </xpath>
        </template>
    </data>
</odoo>
